name: ica11
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.9.2

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8

  # --- Added dependencies for your app ---
  sqflite: ^2.0.0+3
  path: ^1.8.0
  image_picker: ^0.8.4+4
  http: ^0.13.3
  flutter_image: ^4.1.13

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
